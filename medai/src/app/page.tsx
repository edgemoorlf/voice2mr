export default function RootPage() {
  // This page will typically be redirected by the middleware
  // to the user's preferred locale (e.g., /en, /es).
  // You can keep it minimal or add a loading indicator if needed.
  return null; 
} 